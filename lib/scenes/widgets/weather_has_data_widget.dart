import 'package:flutter/material.dart';

class WeatherHasDataWidget extends StatelessWidget {
  const WeatherHasDataWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
